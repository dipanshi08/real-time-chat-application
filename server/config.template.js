//This is a template for the config file.
var config = {};

config.nodemailer = {};
config.nodemailer.mail = "sender mail ID";
config.nodemailer.password = 'password';

config.mongoURI = "mongodb database connection ID";

module.exports = config;